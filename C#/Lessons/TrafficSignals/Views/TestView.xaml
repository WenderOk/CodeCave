<Window x:Class="TrafficSignals.Views.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficSignals.Views" 
        xmlns:viewmodels="clr-namespace:TrafficSignals.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:TestViewModel}"
        mc:Ignorable="d"
        Title="TestView" Height="450" Width="800">
    <StackPanel  VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <Ellipse Width="90"
                        Height="90"
                        Stroke="Black"
                        StrokeThickness="2"
                        Fill="{Binding RedLight}"/>      

                <Ellipse Width="90"
                       Height="90"
                       Stroke="Black"
                       StrokeThickness="2"
                       Fill="{Binding YellowLight}"/>

                <Ellipse Width="90"
                        Height="90"
                        Stroke="Black"
                        StrokeThickness="2"
                        Fill="{Binding GreenLight}"/>
            </StackPanel>
 
            
            <StackPanel >
                <Label Content="Название" FontSize="24"/>
                <TextBox Text="{Binding CurrentName, Mode=OneWay}" IsReadOnly="True" FontSize="24"/>
                <Label Content="Описание" FontSize="24"/>
                <TextBox Text="{Binding CurrentDescription, Mode=OneWay }" IsReadOnly="True" FontSize="24"/>
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <Button Content="Переключить" Command="{Binding SwitchCommand}" FontSize="24"/>
            <Button Content="Выйти" Click="Button_Click" FontSize="24"/>
        </StackPanel>
    </StackPanel>
</Window>
