<Window x:Class="Wpf_observation_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_observation_1.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainVM, IsDesignTimeCreatable=True}"
        Title="MainWindow" Height="650" Width="800">
    <Window.DataContext>
        <local:MainVM/>
    </Window.DataContext>
    <DockPanel>
        <Label Content="Приложение для ведения записей наблюдения" DockPanel.Dock="Top" HorizontalAlignment="Center" FontSize="20" Height="50"/>
        <StackPanel>
            <Button Content="Создать новую запись" Margin="15" Command="{Binding OpenCreateCommand}"/>
            <Button Content="Работа с базой данных" Margin="15" Command="{Binding OpenWorkBDCommand}"/>
            <Button Content="TestConnection" />
        </StackPanel>
    </DockPanel>
</Window>
